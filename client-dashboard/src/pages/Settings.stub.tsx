import ProjectRail from "@/components/chrome/ProjectRail";

export default function Settings() {
  return (
    <div className="flex">
      <ProjectRail />
      <main className="flex-1 px-12 py-10 mono text-[11px] uppercase tracking-[0.18em] text-mute">
        Settings · stub.
      </main>
    </div>
  );
}
